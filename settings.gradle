pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id 'com.github.node-gradle.node' version "3.1.1"
    }
}

rootProject.name='ynab-splitter'
include 'frontend'
include 'ynab-swagger-client'
